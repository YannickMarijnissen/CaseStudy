<Window x:Class="wpf.Views.CalendarView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:wpf.Views"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Calendar View" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Calendar -->
        <Calendar Name="calendar" SelectionMode="SingleRange" DisplayDate="{Binding DisplayDate}" />

        <!-- Task List -->
        <ListBox Name="taskListBox" Grid.Row="1" ItemsSource="{Binding Tasks}" DisplayMemberPath="Name" />
    </Grid>
</Window>
